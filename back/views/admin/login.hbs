<div class="conteiner d-flex align-items-center justify-content-center min-vh-100">
    <div class="contenedor-form-login row">

        <div class="form-login-izquierda col-md-5 d-none d-md-flex">
            <h5>Bienvenido a <span>MARUSST</span></h5>
            <p>Por favor, ingresa tus credenciales para continuar.</p>
        </div>

        <div class="form-login-derecha col-md-7 col-12">
            <h2>Login<span class="d-md-none mb-4">MARUSST</span></h2>
            <form action="/admin/login" method="post">
                {{#if error}}
                <p class="msj-error">El usuario o contraseña son incorrectos</p>
                {{/if}}

                <div class="form-login-group">
                    <div class="contenedor-icono">
                        <i class="fa-solid fa-user"></i>
                        <input type="text" name="usuario" placeholder="Usuario">
                    </div>
                </div>

                <div class="form-login-group">
                    <div class="contenedor-icono">
                        <i class="fa-solid fa-key"></i>
                        <input type="password" name="password" placeholder="Contraseña">
                    </div>
                </div>

                <div class="contenedor-boton">
                    <button type="submit" class="boton">Ingresar</button>
                </div>
            </form>

            <div class="contenedor-registrar">
                ¿No tienes cuenta? <a href="#">Regístrate</a>
            </div>
        </div>
    </div>
</div>